# AI Pattern Analysis Script Requirements - ENHANCED EDITION
#
# Core dependencies for analyze_ai_patterns.py
#
# This script analyzes manuscripts for AI-generated content patterns
# across 14 dimensions using state-of-the-art NLP methods:
#
# CORE: Perplexity, Burstiness, Structure, Voice, Technical, Formatting
# ENHANCED: Syntactic, Sentiment, Bold/Italic, Lists, Punctuation, Whitespace, Code, Headings
# ADVANCED: GLTR token ranking, DetectGPT, RoBERTa AI detection, HDD lexical diversity

# ============================================================================
# BASIC INSTALLATION (Minimal Dependencies)
# ============================================================================

# Advanced readability metrics (Flesch Reading Ease, Flesch-Kincaid Grade, etc.)
# If not installed, script will skip readability analysis
textstat>=0.7.3

# ============================================================================
# ENHANCED INSTALLATION (All NLP Libraries)
# ============================================================================

# NLTK - Enhanced lexical diversity (MTLD) and basic sentiment
# Provides: Moving Average Type-Token Ratio, stemming
nltk>=3.8.0

# TextBlob - Simplified sentiment analysis and n-gram extraction
# Provides: Alternative sentiment analysis, n-gram patterns
textblob>=0.17.1

# spaCy - Syntactic pattern detection and POS tagging
# Provides: Dependency parsing, morphological analysis, subordination detection
spacy>=3.7.0

# Textacy - Advanced stylometric analysis
# Provides: Automated readability index, additional diversity metrics
# Requires spaCy to be installed first
textacy>=0.13.0

# Marko - Markdown AST parsing for Phase 3 structure analysis
# Provides: AST-based blockquote, link, list, and code block pattern detection
marko>=2.0.0

# Transformers - Perplexity, GLTR, sentiment, and AI detection
# Provides: DistilGPT-2 perplexity, RoBERTa sentiment, AI classifiers, GLTR token ranking
# WARNING: Large download (~500MB+ for models), requires PyTorch
torch>=2.0.0
transformers>=4.35.0

# SciPy - Advanced statistical analysis
# Provides: HDD (Hypergeometric Distribution D) lexical diversity, Yule's K
# Required for most robust lexical diversity metrics
scipy>=1.11.0

# Sentence Transformers (Optional) - Semantic embeddings
# Provides: Sentence-level semantic analysis, clustering
# Uncomment if needed for advanced semantic detection
# sentence-transformers>=2.2.0

# ============================================================================
# POST-INSTALLATION SETUP
# ============================================================================

# After installation, download required models:
# python -m nltk.downloader punkt punkt_tab vader_lexicon
# python -m spacy download en_core_web_sm
# python -m textblob.download_corpora

# Transformers models will auto-download on first use:
# - distilgpt2 (~260MB) - Perplexity calculation
# - cardiffnlp/twitter-roberta-base-sentiment (~500MB) - Sentiment analysis
# - roberta-base (~500MB) - AI detection classifier (if using pretrained)
# Total: ~1.3GB for all transformer models

# ============================================================================
# INSTALLATION OPTIONS
# ============================================================================

# Option 1: Minimal (basic analysis only)
# pip install textstat

# Option 2: Enhanced (all NLP features)
# pip install -r requirements.txt
# python -m nltk.downloader punkt punkt_tab vader_lexicon
# python -m spacy download en_core_web_sm

# Option 3: Maximum (including Transformers for true perplexity)
# pip install -r requirements.txt
# python -m nltk.downloader punkt punkt_tab vader_lexicon
# python -m spacy download en_core_web_sm
# Note: Transformers will auto-download GPT-2 model on first use (~500MB)

# Standard library dependencies (included with Python 3.7+):
# - re (regex pattern matching)
# - statistics (mean, stdev calculations)
# - argparse (CLI interface)
# - pathlib (file path operations)
# - dataclasses (structured data containers)
# - json (JSON output format)
# - typing (type hints)
# - collections (Counter for frequency analysis)

# Python version requirement: 3.7+
# Recommended: Python 3.9+ for best compatibility with all libraries
